# xlsynth release guidance

When preparing, reviewing, or integrating release changes in this fork, read and
follow the [xlsynth release conventions](docs_src/releasing.md). They cover branch
history and versioning for `release-base` and `release-candidate`.
