# xlsynth release guidance

When preparing or reviewing a release from this fork, read and follow the
[xlsynth release version convention](docs_src/releasing.md#xlsynth-release-versions).
It applies to release work on `release-base` and `release-candidate`.
